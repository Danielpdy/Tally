import React from 'react';
import { useSession } from '@node_modules/next-auth/react';
import TransactionsPreview from '@app/components/transactions/TransactionsPreview';

const page = () => {

  const { data: session, sta}
  if (!)
} 
export default page
