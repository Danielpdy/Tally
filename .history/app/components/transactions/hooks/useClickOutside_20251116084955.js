import { useEffect } from "react"


const useClickOutside = (ref, callback) => {
  useEffect(() => {
    const handler = (e) => {
        if(!ref.current)
    }
  })

}

export default useClickOutside
